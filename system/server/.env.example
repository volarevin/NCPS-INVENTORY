# Database (required in production — use Hostinger MySQL values, not localhost)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ncps_db

# Server
PORT=5000
NODE_ENV=development

# Required for login tokens — use a long random string in production
JWT_SECRET=your_jwt_secret_key_here
